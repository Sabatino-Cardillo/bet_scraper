services:
  - type: worker
    name: bet-scraper
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bet_manager.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
